# 新对话指引（v1.0 · 2026-09-22 @desk）

> **出处**：船长 2026-09-22 指令——"如果有这样的情况可以新开一个主题对话任务，但应该合并到大主题里面，把新开的对话任务的记忆、产出、文件等，并到原来的大主题下（可以用目录分类）……你还应该生成一个『新对话指引』，把文件怎么放、其他对话怎么调用这些产出都说清楚，以及怎么留协作钩子等等。"
> **配套**：`机制任务清单.md`（A8 = AI 主动识别主题升格；A10 = 本指引）｜`MEMORY_SYNC.md`（记忆分层与分流表）｜`接续状态.md`（水位线）
> **一句话**：**新对话只是大主题的一个"工作间"，不是新房子。产出必须回流大主题。**

---

## 一、什么时候该新开对话（A8 判据）

出现**任一**情形，AI **必须主动提案**（船长不必自己发现）：

1. 某主题在现有对话里长到**"必须每次全读"的体量**；
2. 该主题**跨月持续演进**，每次回话都要先补一大段背景；
3. 工作区 `MEMORY.md` 为它**反复加重**（这是最直接的信号）；
4. 一个对话里**并行推进 ≥2 条互不依赖的长线**。

**不必新开**的情形（别过度切分）：
- 阶段性任务、一次做完就结束的；
- 只是**补充上一轮**（船长连发消息 = 补充，不另开）；
- 同一主题的**连续追问**（留在原对话，机制已能接续）。

---

## 二、新对话的定位与开场（三步）

**第 1 步 · 命名**：`大主题名 · 子任务名（日期）`
> 例：「教育理论 · 内在资源四层结构打磨（2026-09-22）」

**第 2 步 · 开场首条声明**（写进新对话第一条，让未来任何 AI 都能定位）：
```
本对话归属大主题：【大主题名】
上游文件：<主题索引 / MEMORY_SYNC_{主题} / 接续状态.md 位置>
本对话任务：<一句话>
产出落点：<大主题目录下的哪个子目录>
```

**第 3 步 · 挂到主索引**：在大主题的索引文件里加一行登记（见 §四），**登记完才算新对话正式成立**。

---

## 三、文件怎么放（目录分类 · 回流大主题）

**铁律三条：**
1. **产出不放对话里，放文件里**——对话会被压缩，文件不会。
2. **一律落进大主题的目录树**，**不新建同级别的"大目录"**。
3. **文件名带日期前缀**（`YYYY-MM-DD_内容.md`）——日期就是将来回捞的锚点。

**目录模板（以大主题为根）：**

```
大主题/
├── 索引.md                 ← 唯一入口（章节规划 / article-index / 目录清单）
├── 状态.md                 ← 当前结论、未闭环点（可覆写，短）
├── 讨论记录/
│   └── YYYY-MM-DD_子话题.md
├── 产出文章/
│   └── YYYY-MM-DD_标题.md
├── 素材/
└── 子任务_XXX/             ← 新对话的产物落这里
    ├── 产出.md
    └── 讨论记录.md
```

**两个大主题的现成落点：**
| 大主题 | 根目录 | 索引文件 | 子任务目录命名 |
|---|---|---|---|
| 教育 | `家庭教育知识库\` | `article-index.md`、`core-concepts.md` | `子任务_<子话题>\` |
| 固收 | `固收产品分析\` | 主题索引 | 同上 |
| 元认知/协作 | `github\` + `32B母数据\03_元认知跨域\` | `接续状态.md`、`机制任务清单.md` | — |

---

## 四、其他对话怎么调用这些产出（唯一入口原则）

**唯一入口 = 大主题的索引文件。** 不靠"记得"，不靠"翻目录"。

1. **新对话产出后，必须回写索引**——在索引里加一行：
   ```
   | 日期 | 标题/主题 | 一句话结论 | 相对路径 |
   ```
2. **别的对话要引用，只引用索引里登记过的路径**；索引里没有的，视为"不存在"（防止引用到半成品）。
3. **跨主题引用**（如教育文章要用到元认知结论）：**引用索引行 + 路径**，并注明"引自 <大主题> / <日期>"。
4. **训练数据引用**：注明 `源文件 + 日期 + 段落锚点`，供 `conversation_search` 或原文回捞。

---

## 五、协作钩子怎么留（四类，缺一不可）

> 钩子 = 让**下一个对话/另一台机器**能接上的接口。不留钩子 = 产出变成孤岛。

| 钩子 | 写在哪 | 写什么 |
|---|---|---|
| **① 水位线钩子** | `接续状态.md` 该主题行 | "LOG 读到第几行 + 一句话现状 + 下一步"（**可覆写、≤8 行**） |
| **② 待办钩子** | `机制任务清单.md` 或 `ISSUES_{主题}.md` | 未闭环点，**带编号**，状态标 `待确认 / 可执行` |
| **③ 回捞钩子** | 讨论记录 / LOG 的条目头 | `[日期 + 关键词 + 对话标识]`，供 `conversation_search` 回捞原文 |
| **④ 训练数据钩子** | 收束时归入对应库 | 教育/元认知 → `deepseek-7b\inbox\`；固收 → `qwen-7b\inbox\`；**并注明源文件+日期** |

**额外一条（本批特有）**：涉及**32B 母数据**的产出，同时在其 `00_规范与索引\` 里留一行（如更新 `待讨论点清单.md`）。

---

## 六、收束时怎么"合并回大主题"

子对话收束时，动作清单**照常**（讨论记录 / 思想资产 / 训练数据 / 索引 / persona / 同步），只多三层回流：

1. **产出回流**：文件落进大主题的 `子任务_XXX\`（或在对应子目录内）；
2. **索引回写**：大主题索引加登记行（§四）；大主题的 `MEMORY_SYNC_{主题}.md` 加一句 **"含子对话 X 的产出（日期）"**；
3. **水位线更新**：`接续状态.md` 该主题行更新——这样**大主题下次接续时，自动带出子对话的成果**。

---

## 七、一页速查

| 问题 | 答案 |
|---|---|
| 该新开吗？ | 见 §一 四条判据；AI 主动提案 |
| 新对话叫什么？ | `大主题名 · 子任务名（日期）` |
| 产出放哪？ | **大主题目录树内的子目录**，绝不新建同级大目录 |
| 文件怎么命名？ | `YYYY-MM-DD_内容.md`（日期=回捞锚点） |
| 别人怎么找到？ | **只认大主题索引里登记过的路径** |
| 怎么合并回去？ | 产出回流 + 索引回写 + 水位线更新（§六） |
| 怎么留钩子？ | 水位线 / 待办 / 回捞 / 训练数据（§五） |

---
*版本 v1.0 · 2026-09-22 @desk · 对应清单 A8 / A10*
