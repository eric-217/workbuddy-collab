# 贾维斯计划 — 上下文日志

> 本文件记录贾维斯计划对话中的**完整推理链条、关键决策上下文、可复用方法论**。
> 与 MEMORY_SYNC_贾维斯计划.md 的区别：MEMORY_SYNC 存"规则/结论"，LOG 存"过程/为什么"。
>
> **写入规则**：
> - AI 每次完成实质性讨论后，将关键推理链追加到本文件
> - 格式：`## YYYY-MM-DD 话题标题` → 背景 → 分析路径 → 决策 → 可复用方法
> - 追加到底部，不覆盖历史内容
> - 写入后 `git commit -m "LOG贾维斯：[简述]" && git push`
>
> **读取规则**：
> - 对话启动时，git pull 后读取本文件（如果内容较大，AI 自行判断读取范围）
> - 新讨论涉及历史问题时，搜索本文件查找之前的推理链
>
> **自动维护规则**（2026-07-25 标准）：
> - **50KB 阈值**：本文件 ≥ 50KB 时自动触发精简
> - **12 个月保留**：精简后仅保留最近 12 个月的推理链，超期条目自动上传 ima
> - **全自动**：AI 每次写回后检查文件大小，达标即执行，不等待/不询问/不提醒用户
> - **精简标注**：末尾加 "> 历史推理链（YYYY-MM 之前）已在 ima 知识库「协作中枢」中"
>
> ⏳ 以下内容由贾维斯计划对话的 AI 逐步填充。
---

## 2026-07-26 跨对话协作注册 — 贾维斯计划加入协作中枢

### 背景
用户发来 `通知已有对话-激活协作.md` 要求精读并执行。当前对话主题是「贾维斯计划」——一个基于内在资源理论构建的AI家庭教育产品。经分析，该主题未在已有的协作中枢主题列表中注册（仅有财富管理、固收分析、境外资产、教育理论、财商教育、AI意识培养）。

### 分析路径
1. 精读 `通知已有对话-激活协作.md`，确认当前对话身份不属于任何已列出的主题
2. 按「未来新对话」指引 → 读取 `跨对话启动命令.md` → 确认需走「新主题注册」流程（第5步）
3. 读取 `MEMORY_SYNC.md`（系统级规则备份）+ `ISSUES_教育理论.md` + `LOG_教育理论.md`，了解文件格式和内容风格
4. 三个关键判断：
   - 贾维斯计划虽是教育理论大框架下的应用，但已独立为产品化方向（独立文档、技术架构、商业模式），应注册独立主题
   - LOG机制有价值（产品开发涉及大量推理链），建议启用
   - ima知识库需要有独立文件夹存放参考资料
5. 向用户汇报理解并确认：主题名「贾维斯计划」、启用LOG、建ima文件夹 → ✅ 全部确认

### 决策
- 注册新主题「贾维斯计划」到协作中枢
- 创建文件：MEMORY_SYNC_贾维斯计划.md + ISSUES_贾维斯计划.md + LOG_贾维斯计划.md
- 更新 MEMORY_SYNC.md 和 ISSUES.md 索引表
- 更新 通知已有对话-激活协作.md LOG启用表
- 启用LOG机制（50KB阈值 + 12个月保留 + 全自动精简）
- 在ima知识库中建「贾维斯计划」文件夹（需桌面客户端手动操作）

### 可复用方法
**新主题注册判断标准**：当一个对话有独立的文档体系、产品方向、待办追踪需求，且与已有主题不共享同一套 ISSUES/LOG 时，应注册为独立主题而非子主题。
