# A2 分层部署 · 窗口账本

> **一句话**：船长 2026-09-22 令——"你先部署、自检，同时保留可调、可回退的窗口；等确实更好用时，再把这些窗口关上。"
> 本文件就是**窗口的账本**：部署了什么、自检结果、哪个窗口还开着、凭什么是"更好用"了、怎么一键回退。
> **位置**：`github\`（随 git 双机同步）｜**版本** v1.0 · 2026-09-22 @desk｜**评估日：2026-10-06**（与密语试点同期）

---

## 一、部署了什么（三件，全部可回退）

| # | 动作 | 文件 | 性质 |
|---|---|---|---|
| 1 | 固收主题文件**顶部新增 L2 状态头**（≤30 行，"当前得知道什么"一屏可见） | `MEMORY_SYNC_固收分析.md`（第 3–24 行，`L2 状态头 BEGIN/END` 区间） | 纯增量，原内容一行未删 |
| 2 | 新增**分层上限与超限降级**规则（三层各设上限 + 超限去向 + 降级铁律） | `MEMORY_SYNC.md`（「机制设计原则」节之后） | 纯增量文本 |
| 3 | 机制任务清单 A2 状态更新 | `机制任务清单.md` | 状态变更 |

**没做的（这就是"还开着的窗口"）**：文件依旧保持"启动必读"、原全文一字未删、不加任何自动检查——三条都刻意留着，见第三节。

---

## 二、自检结果

| 检查项 | 结果 |
|---|---|
| 状态头行数是否 ≤30 | ✅ 19 行 |
| 标记区间是否可精确定位 | ✅ `grep -n "L2 状态头 BEGIN/END"` 命中共 2 行 |
| 原文件是否受损 | ✅ 1328 → 1364 行（+36），纯增不减；下方章节结构未动 |
| 回退命令是否可用 | ✅ 已 dry-run（见第四节） |
| 状态头是否必然被读到 | ✅ 它所在的文件本身就是"启动必读"，**不需任何新机制** |
| 双机同步 | 见文末状态行 |

---

## 三、开着的三个窗口（凭这个判断能不能关）

| 窗 | 开着的是什么 | 为什么先开着 | **关闭条件（＝确认确实更好用了）** |
|---|---|---|---|
| **W1** | `MEMORY_SYNC_固收分析.md` **仍保持"启动必读"**（未降为"只读状态头 + 按需下查"） | 一旦降级，AI 可能不读→不可逆地丢上下文；先并存观察更稳 | 连续几次固收接续，**只读状态头就能接上、没有脱节感** → 再降为按需 |
| **W2** | 原全文**一字未删**（状态头与正文允许短期重复） | 先保真，不怕冗余 | 观察两周后，若确认状态头已覆盖全部"当前态"、正文重复段确为历史堆积 → 才谈清理 |
| **W3** | 上限规则**只是文本，无自动检查** | 不引入新机制（避免机制自己膨胀） | 若实际出现**≥2 次超限仍未降级** → 再把它挂进收束检查项（届时报批） |

> **判断依据交给船长的手感**，不交给我的论证：接续时有没有"接不上"的感觉、有没有"又得翻半天才找到当前口径"的感觉。这两点变好了，就可以关窗。

---

## 四、回退（逐条，随时可用）

```bash
# ① 只回退 L2 状态头（推荐：保留上限规则）
cd /d/workbuddy/github
sed -i '/L2 状态头 BEGIN/,/L2 状态头 END/d' MEMORY_SYNC_固收分析.md

# ② 整体还原部署前的三个文件
cp /d/workbuddy/备份/A2部署_20260922/MEMORY_SYNC_固收分析.md \
   /d/workbuddy/备份/A2部署_20260922/MEMORY_SYNC.md \
   /d/workbuddy/备份/A2部署_20260922/机制任务清单.md .
```

**部署前副本**：`D:\workbuddy\备份\A2部署_20260922\`（3 文件，110KB/16.8KB/10.8KB）
**另一重保险**：git 历史（本次提交的前一个 commit 即为纯部署前状态）

---

## 五、评估记录

| 日期 | 结论 | 操作 |
|---|---|---|
| 2026-09-22 | 部署 + 自检通过；**三个窗口全部保持开启** | — |
| 2026-10-06（待） | 由船长凭手感判：接上更顺了吗？找当前口径更省事了吗？ | 据此关窗 / 调窗 / 回退 |

*状态：git ✅ `e2f7631`（4 文件 / +108 −3）｜N1 接力推送：见 `memory\2026-09-22.md`*
