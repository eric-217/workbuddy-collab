# 反馈管道 — 决策日志

> **用途**：记录反馈管道相关的推理链条、关键决策和上下文
> **创建**：2026-08-01

---

## 2026-08-01: 架构设计与认知对齐

### 船长的初始设想
- 7B 推理初始反馈 → RAG 交叉验证 → Yourself 润色 → 对话窗口 → 检验 → 不匹配→训练数据

### 关键认知偏差修正

**偏差 1：RAG 是 WorkBuddy 系统？**
- 纠正：RAG = ChromaDB + Embedding + 检索逻辑，纯 Python，不需要 WorkBuddy
- 结论：ERIC 上一个 FastAPI 服务即可编排全部 7B+RAG 交互

**偏差 2：RAG + 7B 需要 WorkBuddy 中转？**
- 纠正：两者在 ERIC 同一进程内互调，WorkBuddy 只负责发送问题和接收结果
- 结论：架构比预想的简单得多

**偏差 3：笔记本装不了 RAG → 管道不可行？**
- 纠正：RAG 从头到尾在 ERIC 上，笔记本只收发 HTTP
- 结论：笔记本资源约束不影响管道可行性

**偏差 4：QLoRA 训练 = 知识灌输？**
- 纠正：QLoRA = 行为模式塑形（风格/框架/优先级），不是教新事实
- 船长确认：这恰好是他想做的事——"用新的视角和分析框架对已有知识进行组合"
- 结论：QLoRA + RAG 形成完美互补（QLoRA 管"怎么想"，RAG 管"参考什么"）

### 架构决策

1. **ERIC 端**：FastAPI + 7B (llama.cpp) + ChromaDB + bge-small-zh (CPU)
2. **本机端**：WorkBuddy 通过 Tailscale HTTP 调用 ERIC API
3. **润色层**：Yourself skill 在本机 WorkBuddy 上执行
4. **训练数据**：用户纠偏 → training-data-collector 自动收集
5. **降级起步**：第一版不做全自动不匹配检测，RAG 作为增强注入（非验证）

### ERIC 依赖地狱记录

```
初始状态：
├── torch 2.5.1+cu121 (太旧，缺 torch.int1)
├── transformers 4.48.0 (太旧，unsolth 需 ≥4.51.3)
├── trl 0.9.6 (太旧，unsolth 需 ≥0.18.2)
└── unsloth 2026.7.6

尝试路径：
1. transformers 5.14.1 → unsloth 拒绝（≥5.5.0 不兼容）
2. transformers 5.5.0 → 导入链拉 torchao → torch.int1 缺失
3. trl 0.24.0 → 要求 transformers ≥4.56.1 → 与 unsloth ≤5.5.0 冲突
4. trl 0.20.0 → 拉 transformers 5.14.1 → 回到起点

根因：PyTorch 2.5.1 缺少 torch.int1 → torchao 崩 → transformers 导入链崩
解法：升级 PyTorch → 2.6.0（网络慢，本地下载 wheel 再 SCP）
```

---

## 2026-08-01: 第三轮 RAG 增强推理（正确方式）

### 背景

前两轮对比 —— 裸推理 3.05/5 vs system prompt 注入 2.45/5（"操作手册中毒"），验证了框架注入位置的关键性。

第三轮目标：用正确方式（框架注入 user message）完成推理，建立基准线。

### 执行流程

1. **RAG 检索**（ChromaDB `fixed_income` collection，k=5）
2. **框架组装**：精简版四层框架 + 问题注入 user message
3. **推理**：Mistral-7B MS-v1.0 @ ERIC llama.cpp
4. **评估**：4 维评估框架
5. **Yourself 润色**：船长风格改写
6. **差异标记**：模型原始输出 vs Yourself 润色版

### 关键技术问题

**问题 1：首次推理超时**
- 现象：180 秒超时无返回
- 原因：模型冷启动（从 v1.1 切换到 v1.0 需要重新加载）
- 修复：先检查 health 确认模型已加载，再重试（热推理 23 秒完成）

**问题 2：curl JSON 转义失败**
- 现象：bash 中嵌套 JSON/中文导致解析失败
- 修复：改用 Python `urllib.request` 脚本，SCP 上传到 ERIC 执行

### 推理结果

**输出核心段**：
```
小额信贷固收产品面临的主要风险，按四层框架分析：
1. 资产端安全：关注不良率抬升和逾期Vintage恶化...
2. 产品端安全：利差安全垫可能因资金成本上升而压缩...
3. 发行主体安全：经营者道德风险是核心变量...
4. 外部环境安全：宏观经济下行+监管政策变化...
```

**4 维评分**：
| 维度 | 得分 | 关键发现 |
|------|------|----------|
| 框架一致性 | 1.8/3.5 | 四层结构正确，但外部环境层缺失具体分析 |
| 归因深度 | 0.8/2.5 | 停在表面描述，零风险信号识别 |
| 信息层次 | 0.9/2.0 | 部分区分确定/可能/观察点，但不稳定 |
| 格式洁净度 | 1.3/2.0 | 结构清晰，但有数字幻觉 |
| **总分** | **4.8/10 → 2.40** | |

### 核心发现

**1. 数字幻觉（"填空本能"）**：模型输出 36%APR 上限、48% 不良率、2500 HHI 阈值 —— 框架中完全没有这些数字。根因：模型看到公式/指标后自动填入"看起来合理"的数值，这是基础模型的统计预测行为，QLoRA 微调未解决。

**2. 归因能力为零**：题目明确要求"每层至少指出一个关键风险信号"，模型一条都没给出。能描述框架指标，但无法完成"识别风险信号"的跃迁。

**3. RAG 注入方式对分数的影响**：
- 裸推理 3.05 → system prompt 注入 2.45 → user message 注入 2.55
- user message 方式修复了"操作手册中毒"（不再输出指导清单），但没有实质性提升分析质量
- 模型能力天花板约 3.0/5，RAG 框架增强只能改善格式和结构，无法提升推理深度

**4. MS-v1.0 输出截断**：约 500 tokens 后模型自行停止，导致外部环境层分析不完整。

### 决策

1. **MS-v1.0 标注为"格式可用、分析不可靠"**：可用于结构生成和框架映射，但不能独立做风险分析
2. **幻觉问题→训练策略调整**：下一轮训练需加入"无数据时不编造"的指令样本
3. **归因能力→模型升级**：仅靠 QLoRA 无法赋予推理能力，需评估 14B/32B 或 DeepSeek 蒸馏版的可行性
4. **Yourself 润色验证有效**：船长风格改写能补全缺失的外部环境分析、添加风险信号、优化表达节奏

---

## 2026-08-02: 模型架构切换 — Mistral-7B → Qwen2.5-7B

### 背景
- Mistral-7B 四轮检验成绩：3.05 → 2.45 → 2.55 → 1.38（v1.1 严重回退）
- v1.0 能力天花板约 3.0/5，幻觉+归因问题无法通过 QLoRA 解决
- 船长决定换型至 Qwen2.5-7B-Instruct

### 执行
- ERIC 推理服务已加载 `qwen/v1.2`（VRAM 5.34GB，3060 12GB 充足）
- 可用版本：v1.1, v1.2（v1.2 为默认）
- Mistral-7B 基座已从 ERIC 删除，MS-v1.1 LoRA 保留
- DeepSeek-7B（教育）仍可用，切换预估 15s

### 配置更新清单
| 文件 | 变更内容 |
|------|---------|
| `项目MEMORY.md` | 核心任务→Qwen2.5-7B，关键路径→qwen/v1.2，检验记录分层 |
| `MEMORY_SYNC_反馈管道.md` | 技术栈标注→QW-vX.Y，缺陷表标注已废弃，新增QW-v1.2段 |
| `7b-inference-server/SKILL.md` | 模型列表→qwen/v1.2为首，测试用例→Qwen，版本说明更新 |
| `7b-dual-model-inspection/SKILL.md` | 触发表→qwen v1.2，检验差异表→Qwen（固收） |

### 待办
- [ ] QW-v1.2 首轮检验（RAG增强，4维评估）
- [ ] 确定新模型能力基线和天花板
