# 讨论记录：记忆不是「存了」，是「接得上」

**日期**：2026年9月21日–22日
**主题**：跨机/跨日接续机制 —— 从"记忆同步"到"上下文加载"，再到分层与检索
**来源**：WorkBuddy「功能架构」对话（台式机 desk，跨 09-21 晚～09-22 上午）
**性质**：元层讨论（协作机制），非固收/教育主题内容

---

## 零、本轮的起点与自我修正

船长提出：「当前的同步只能做到对话记录上的同步，但无法做到记忆上的同步，是吗？也就是说，A电脑上的AI实际并不知道我们在 B电脑上推进了什么？」

**AI 第一轮回答（后被修正）**：查 `n1_sync.py` 源码实证后给出——"记忆数据早就跨机同步了（config 块含用户级 MEMORY.md、wsmem 块含各工作区记忆、云端画像服务端共享），缺口在**加载**不在**同步**"。并给出建议：不新建 skill，升级 `user-context-loader` 加「开始今天的工作」触发分支。

**船长第二轮修正（本节的核心）**：

### 船长观点（原话）

> "我认为我们对记忆的理解有一点错位，你靠文件（产出）来记忆，但我其实还希望你把上下文也加进去，而这刚好是你有上限的地方，比如你刚刚说的'工作区 MEMORY.md 已经膨胀超限，这次会话注入时被截断了'。"
>
> "在当前这个'功能架构'对话里，你精简重写问题不大，因为每一项任务几乎都是阶段性的，做好就完成了，依赖文件（产出）本身是很好的机制。但是，针对固收和教育两个主题，这样的机制就会有很明显的限制，因为这两个主题我有很明显的思想体系，要讨论和生成的内容都要求是连贯的，不能断，要不然就会有非常明显的前后脱节的感觉。"
>
> "目前我已经建立了CFG和CPG两个skill来减少这些损失，但仍不能彻底解决，这导致我要花大量时间来跟你更新协作机制，且协作机制也同时在膨胀，有一天可能也不得不精简。"

### AI 分析（修正后）

AI 的第一轮回答把问题答在**事实层**（数据在不在），船长的追问在**机制层**（记忆是什么）。两者不是一回事：

- 事实层：数据同步 ✅、加载动作 ❌ → 这一层 AI 答对了，且已落地（水位线机制）。
- 机制层：**"记忆"= 文件产出 + 上下文**，两套。阶段性任务（工具、部署、一次性工程）靠文件产出就够；有思想体系、要求连贯的主题（固收、教育）必须两套都有。

AI 起初把"数据已同步、只缺加载动作"固化为结论，等于默认了"记忆=文件"。船长指出这是"错位"。修正后的口径：**被截断的注入（MEMORY.md 超限）只是这套机制在短对话里暴露的可见症状，不是根因；根因是记忆机制没有按主题性质分档。**

---

## 一、船长的两个问题（AI 一并作答）

### 问题1：有没有更好的办法？（要求全网检索）

**船长观点**：
> "1、这有更好的解决办法吗？我想让你做一个全网检索，看看其他人是怎么做的，有哪些更好的办法？2、把上下文本身做一个文件产出，按日期或时间做标记，这是否会有帮助？"

**AI 分析**（全网检索三支流派）：

| 流派 | 代表 | 核心机制 | 对船长的适用性 |
|---|---|---|---|
| OS 式分层记忆 | MemGPT / Letta | 上下文=内存、归档=外存，模型自决换入换出，"记忆块"可自编辑 | 思想可用，但需常驻服务 |
| 抽取管道 | Mem0（LOCOMO 论文） | 自动从对话抽取事实 + 增删改，省 90%+ token | 中文垂直领域抽取易失真 |
| 时序知识图谱 | Zep / Graphiti | 每条事实带**有效期**，能分辨"当时为真 vs 现在为真" | 理念最贴近需求 |

工程实践派（更贴船长）的共性只有三条：**① 原始轨迹层 / 提炼状态层 / 索引层，三层职责分开**（状态文件 ≤100 行、可覆写；日志 append-only、永不合并）；**② 不全量灌上下文，按需检索**；**③ 摘要不替代原文**——保留能回捞原文的指针。

**结论**：CFG 的 `MEMORY_SYNC / ISSUES / LOG` 三件套**方向本来就是对的**，等于工业界的"状态/任务/轨迹"分层。缺的只是：状态层没有硬上限（110KB，被当轨迹用了）、没有水位线、原文没有回捞指针。**不建议引入 Mem0/Letta/Zep**——瓶颈不是"存不下"，是"接不上"，文件 + 水位线足够。

### 问题2：把上下文做成按日期的文件产出，有帮助吗？

**AI 分析（本轮讨论中）**：有帮助，而且已经在做（`LOG_{主题}.md` 就是它）。但要按三种职责切，把"按日期"配上"按主题索引 + 水位线"：

- **轨迹层**：`LOG_{主题}.md` — 追加、按日期、永不删
- **状态层**：`接续状态.md` — **可覆写**、每主题 ≤8 行
- **索引层**：主题 → 文件 → 水位 的表

**船长追问（本节新增，是本轮讨论真正打开的一层）**：
> "是否可以把每个对话的记忆文件改成动作，而非内容呢？比如，你现在的记忆文件里写的应该是我们说了什么，是否可以把我们说了什么形成完整、详细的文件，在记忆文件里只记录对话要做什么，比如先读什么，后读什么？"
> （并明确："先讨论再执行"）

**AI 分析**：可行、方向对，但它不解决"自动执行"——动作文件只是指令文本。切法不是"记忆=动作、内容=全量"两分，而是三层：

| 层 | 内容 | 体量约束 | 读取方式 |
|---|---|---|---|
| **L1 动作层** | 先读什么、后读什么、什么条件下读哪个 | ≤200 行 | 每次全读 |
| **L2 状态层** | 当前结论 / 未闭环 / 水位 | 每主题 ≤100 行，可覆写 | 每次全读 |
| **L3 内容层** | LOG、讨论记录、原文、训练数据 | 不限 | 按需检索 |

**一条硬限制**：动作文件**不能取代结论**。若把结论全推到内容层，AI 每次得先读内容才能判断——反而更慢更耗。所以 L2 必须留少量结论，L1 只留"读哪"。

**状态：待船长确认（未定论，已记入 `机制任务清单.md` A2）**

---

## 二、船长的两个使用习惯（已写入机制，非"记在心里"）

船长原话：
> "我一般不会新建对话，而是在现有对话里继续推进，这样既方便我工作，也不会让界面混乱，时间长了都不记得是在哪个对话里推进的了。所以，你说的'新建对话'是指在现有对话里继续的意思吗？…总之，我一般不新建对话任务，如果有，那就是要长期使用的。"
>
> "有些对话任务我有可能在A电脑推进后，要等很多天才会在B电脑上继续推进，所以这个skill要把这种情况也考虑进去，不要加时间限制，或用其他的更新条件。"

**处理**：AI 上一轮说的"新建对话"是错的，已改。CPG 1G 与 `user-context-loader` 里都写死：
- **不依赖新建对话**：本机制在任何现有对话内触发即执行
- **无时间限制**：判据只有水位线（读到第几行），**明令禁止用"近 N 天"筛选**；隔 1 天与隔 30 天同样全读

---

## 三、RAG 系统：重新评估（长悬问题）

**船长原话**：
> "我想起有一个rag系统好像可以适当缓解我的问题，但因为笔记本的配置可能不够，导致这个系统实际从没有运行过，我也不知道这个系统是否能解决我的需求。"

**AI 实测修正（三处）**：

1. **它其实跑起来过**：`server.log` 显示 7/11–7/12 服务正常响应（大量 200），索引也建成了——289 文件 / 14,761 chunks / 768 维（`BAAI/bge-base-zh-v1.5`）。失败的只是 **7/23 的重建**。
2. **卡在维护，不是配置**：7/23 重建失败的三个坑 = `build.py` 调用路径错 + 日志 gbk 编码报错 + tokenizer "Already borrowed"（并发）。台式机 16GB 够跑（系统本来就是按 8GB 内存优化的，embedding 走 CPU 不抢显存）。
3. **它当年"不好用"的真实原因**：索引里 `对话全记录` 占 **13,435/14,761 = 91% 的 chunk** → 检索被对话原文淹没，把书稿/理论挤下去。

**船长的理解性追问（本轮新增，待答）**：
> "关于这个RAG系统重建，我一直没理解，是有更新就要重建吗？如果是，那这个频率太多了吧，我几乎每天都有更新，那每天都要花时间重建一次？"

**AI 分析（本轮）**：按现在的 `build.py` 确实是全量重建（收集全部 → 全部编码 → 整体覆盖 `embeddings.npy` + `metadata.pkl`），改一个文件也要重跑全库。**但这是脚本设计，不是必须**。三条省法：
- **a) 改增量索引**：按文件 mtime/hash 对比，只对新/改动文件算向量、追加到库尾，删除的文件标记失效（约几十行代码）。
- **b) 缩小索引范围**：把 `与deepseek对话全记录/` 移出索引 → 14,761 → 约 1,300 chunks，全量重建从十几分钟降到 **1 分钟以内**，且检索质量立刻变好。**这一条即使不改代码也能解决频率问题。**
- **c) 按需重建**：索引只是"回捞细节"的工具，可定成每周一次 / 只在书稿定稿时建一次。

**关键认知**：重建是**电脑的活，不该占船长的时间**——放计划任务（夜里），人不需要参与。现在觉得"每天要重建"，是因为它从没被自动化、且范围太大。

**状态：待船长确认（已记入 `机制任务清单.md` B1–B6）**

---

## 四、交错时段的两轮（09-21 晚～09-22 上午，N1/N2 架构）

### 船长观点（原话）

> "我突然意识到，好像没有必要在办公室和家里各部署一台n1盒子来达到台式机和笔记本同步的目的…两个地方各用一个盒子有什么特别的好处吗？"
>
> "就是推的话往两个盒子上同时推，但拉的话只从n1上拉，你认为呢？"
>
> "n1大部分时间是没有新数据的…让n2每隔半小时或1小时执行一次拉取是否有点浪费？…只需确保每天一样就好了。或者，是否可以让n1建一个简单的向n2的推送任务：告诉对方'我有更新，你可以来拉'"
>
> "如果向n2推送失败，则留下命令让n1向n2推，可以吗？或者反过来也可以"

### AI 分析

AI 起初的设计是"**双向** rsync 全同步"和"**定期**（半小时/小时）拉取"——对称、等距、好理解。船长两轮把它改成：

| 维度 | AI 原设计 | 船长改判 |
|---|---|---|
| 方向 | 双向互推互拉 | **读只从权威源（N1）单向；写可多路（双机→N1、双机→N2）** |
| 频率 | 固定周期轮询 | **由"数据有没有变"决定**（事件触发 + 每日 05:25 兜底 + @reboot） |
| 失败 | 隐含"下次会重试" | **显式留下兜底路径**（推失败则留命令，或反向推） |
| 部件价值 | 两地各一盒子（对称） | **先算边际价值**——N2 定位改为"家庭服务器 + 温备件"，同步价值承认边际 |

### 核心判断（AI 归纳）

船长这一串动作的共同结构是：**用"条件"替代"节拍"，用"单向"替代"对称"，用"显式兜底"替代"隐含重试"。** 与固收里的「刺-收循环」「单一路径依赖」同源。

---

## 核心共识

1. **"记忆"= 文件产出 + 上下文，两套**；阶段性任务靠文件产出即可，有思想体系、要求连贯的主题（固收、教育）必须两套都有。
2. **同步 ≠ 加载**：数据早就在两台机器上，缺的是"读到哪"这个断点；断点用水位线，不用时间窗口。
3. **读只从权威源单向，写可以多路**；频率由"数据有没有变"决定，不由"时间到了"决定。
4. **精简只能发生在可覆写的那一层**（状态层）；轨迹层只增不减、按日期可回捞原文——这是"连贯不断"的机制保证。
5. **RAG 不是配置不够，是维护没做 + 索引范围失控**；它补的是"细节回捞"，不解决"接续"。

---

## 相关待办

- **A1** 上轮 5 处改动三选一（我为"(c) 部分保留"）
- **A2** 分层方案细化定稿（含防膨胀规则）
- **A3** `MEMORY_SYNC_固收分析.md`（110KB）加「当前状态速览」头部
- **A4/A5** CPG（96.6KB）/ CFG（37.9KB）拆分手术
- **B1–B6** RAG：索引范围、增量索引、修三个坑、计划任务、启用与否、README
- **C1** 本对话元认知跨域收数据 ✅ 已完成
- 全表见 `机制任务清单.md`

---

*记录整理：WorkBuddy @desk，2026-09-22*
