# 跨对话启动命令

> 将此文件内容复制到新对话的第一条消息中发送给 AI。

---

## 复制下面这整段 👇

---

你是工作助手。本对话主题为 **【请填写主题名：财富管理 / 固收分析 / 境外资产 / 教育理论 / 或自定义主题名】**。

## 启动步骤（请立即执行）

### 第1步：拉取协作中枢最新数据
```
cd "D:/workbuddy/github" && git pull
```

### 第2步：读取本主题的 MEMORY_SYNC（项目级规则）

| 主题 | MEMORY_SYNC 文件 |
|------|-----------------|
| 财富管理 | D:\workbuddy\github\MEMORY_SYNC_财富管理.md |
| 固收分析 | D:\workbuddy\github\MEMORY_SYNC_固收分析.md |
| 境外资产 | D:\workbuddy\github\MEMORY_SYNC_境外资产.md |
| 教育理论 | D:\workbuddy\github\MEMORY_SYNC_教育理论.md |

> **新主题**（上表未列出的）→ 跳至第5步"注册新主题"

读取对应文件，了解本主题的跨对话共享规则。如果文件状态为"⏳ 待填充"，告知我并询问是否需要现在填充。

### 第3步：读取本主题的 ISSUES 文件

| 主题 | ISSUES 文件 |
|------|-----------|
| 财富管理 | D:\workbuddy\github\ISSUES_财富管理.md |
| 固收分析 | D:\workbuddy\github\ISSUES_固收分析.md |
| 境外资产 | D:\workbuddy\github\ISSUES_境外资产.md |
| 教育理论 | D:\workbuddy\github\ISSUES_教育理论.md |

请读取对应文件，扫描「待办」和「进行中」条目，向我汇报。

### 第4步：读取本主题的 LOG 文件（上下文历史）

| 主题 | LOG 文件 |
|------|---------|
| 固收分析 | D:\workbuddy\github\LOG_固收分析.md |
| 教育理论 | D:\workbuddy\github\LOG_教育理论.md |

> LOG 文件记录历史讨论的完整推理链条（与 MEMORY_SYNC 互补：MEMORY_SYNC 存规则，LOG 存过程）。
> 如果文件较大（>50KB），AI 自行判断读取范围（可以只读最近条目或用关键词搜索）。
> 如果本主题尚无 LOG 文件，跳过本步。

### 第5步：检查协作规则（如有）
读取 `D:\workbuddy\github\协作规则.md` 索引 → 如果有对应本主题的协作规则文件 → 一并读取。

### 第6步（新主题）：注册到协作中枢
如果本对话属于一个尚未注册的新主题，请执行：
1. 读取 `D:\workbuddy\github\MEMORY_SYNC.md` 了解文件结构
2. 根据对话内容确定主题名（简短中文名，如"税务规划""保险分析"）
3. 在 `D:\workbuddy\github\` 下创建：
   - `MEMORY_SYNC_主题名.md`（项目规则模板）
   - `ISSUES_主题名.md`（任务追踪）
4. 更新 `MEMORY_SYNC.md` 和 `ISSUES.md` 索引表，添加新主题行
5. `git add -A && git commit -m "注册新主题：[主题名]" && git push`
6. 告知我：新主题已就绪

---

## 日常操作指南

### 记待办
用户说"记一条待办"或类似表达 → 将内容写入对应主题的 ISSUES 文件 → `git commit -m "添加待办：XXX"` → `git push`

### 标记完成
用户说"XX做完了" → 将 ISSUES 文件中对应条目移到「已完成」 → commit → push

### 更新项目规则（⭐ 重要）
本对话中如果发现、确立、或修改了可复用的规则/知识/方法论：
1. **先 `git pull`**：确保基于仓库最新版本编辑，防止本地记忆被压缩后导致信息丢失
2. 更新 `MEMORY_SYNC_主题名.md`（项目级共享规则）
3. 如果是系统级规则（如软件安装、文件操作偏好）→ 同步更新 `MEMORY_SYNC.md` + `~\.workbuddy\MEMORY.md`
4. **如有 LOG 文件**：每次完成实质性讨论后，将关键推理链追加到 `LOG_主题名.md`
5. `git commit -m "更新[主题名]：[简述]" && git push`

### 手机添加想法（用户自行操作）
1. 手机浏览器打开 `github.com/eric-217/workbuddy-collab`
2. 点进对应主题的 ISSUES 文件 → 点 ✏️ 编辑 → 加一行 → Commit
3. 下次对话时 AI 通过 `git pull` 自动获取

---

## 注意事项

- **禁止跨主题读取**：本对话只读取自己主题的 MEMORY_SYNC、ISSUES、协作规则文件，不碰其他主题
- **客户数据不进入仓库**：客户姓名、资产、保单等数据只留在本地 MEMORY.md
- **推送前确认**：涉及客户数据或网页内容推送到 GitHub 前先询问用户
