# 反馈管道 — 跨对话规则与架构

> ⚡ **收束机制归属（2026-09-04）**：本主题属 **CPG 侧**——说"收束/归档/总结/封盘"立即执行 CPG 全量收束闭环（context-preservation-guard 规则），详见 MEMORY_SYNC.md「收束机制主题分流」。

> **用途**：所有涉及7B本地模型推理、RAG增强、反馈管道的对话启动时读取此文件
> **更新方式**：任一相关对话中发现可复用的规则/架构变更时，更新本文件并 git push
> **状态**：✅ 已激活
> **创建**：2026-08-01
> **最后更新**：2026-08-01

---

## 一、核心架构

```
┌─────────────────────────┐      HTTP (Tailscale)      ┌──────────────────────────┐
│  笔记本 (WorkBuddy)      │ ◄─────────────────────────► │  ERIC 训练机 (100.115.45.85) │
│                         │                             │                          │
│  ① 发起对话/提问        │  ──── query ────►           │  ④ FastAPI 编排器        │
│  ⑤ Yourself 润色        │                             │    ├─ 7B 推理 (QLoRA)    │
│  ⑥ 展示给船长检验       │  ◄─── result ────           │    ├─ RAG 检索           │
│  ⑦ 纠偏 → 训练数据收集  │                             │    │  └─ bge-small-zh     │
│                         │                             │    │  └─ ChromaDB         │
│                         │                             │    └─ 7B 自检 + 合并     │
└─────────────────────────┘                             └──────────────────────────┘
```

**关键认知**：
- RAG 不是 WorkBuddy 系统，是纯 Python 工具包，直接在 ERIC 上与 7B 同一进程内互调
- ERIC 上不需要安装 WorkBuddy，只需一个 FastAPI HTTP 服务
- 本机 WorkBuddy 通过 Tailscale 隧道访问 ERIC API

---

## 二、ERIC 端技术栈

| 组件 | 技术 | 显存 | 说明 |
|------|------|------|------|
| 7B 推理 | llama-server (llama.cpp) + QLoRA adapter | 4-6 GB | QW-vX.Y checkpoint (固收) / DS-vX.Y checkpoint (教育) |
| 中文 Embedding | bge-small-zh-v1.5 (102MB) | CPU | CPU 推理，不占 GPU |
| 向量数据库 | ChromaDB | CPU | 内存模式，轻量 |
| HTTP API | FastAPI + uvicorn | CPU | 编排 7B+RAG 管道 |
| 合计 | — | 4-6 GB | 3060 12GB 完全够用 |

**Embedding 为什么用 CPU**：bge-small-zh 仅 102MB，单次嵌入耗时 ~50ms，检索几百条文档不到半秒。没必要占 GPU 显存。

---

## 三、管道流程（5 步）

### Step 1: RAG 增强检索
```
输入：用户问题 (embedding → 向量检索)
输出：知识库中 Top-K 相关文档片段
```

### Step 2: 框架注入 + 推理（合并步骤）
```
输入：用户问题 + RAG 检索片段（作为用户消息的一部分）
拼接格式：[相关知识] + RAG片段 + [问题] + 用户原始问题
⚠️ 不要将框架注入 system prompt！
原因：system prompt 中的框架列表被模型理解为"操作手册"→ 只输出指导清单不做分析
正确：框架作为"已知信息"拼入 user message → 模型视其为答题材料
输出：7B 模型基于RAG增强的初步回答
```

### Step 3: 比对（AI 执行，不在 ERIC 上）
```
输入：7B 回答 + RAG 片段 + 船长核心框架（AI已知）
操作：检查模型回答是否覆盖 RAG 片段中的关键框架/事实
      标记模型自创概念 vs 已建框架的匹配度
输出：比对结果 + 差异标记
```

### Step 4: Yourself 润色（本机 WorkBuddy）
```
输入：7B 自检后的回答
操作：按船长的思维风格、表达方式、行文习惯调整
输出：最终回答
```

### Step 5: 用户检验 + 训练数据收集
```
用户查看 → 如修改/纠偏 → training-data-collector 自动收集
不匹配 → 标记为下一次训练数据
```

---

## 四、自动不匹配检测（降级版）

| 检测点 | 方法 | 阈值 | 动作 |
|--------|------|------|------|
| 7B 回答 vs RAG 检索 | Embedding 余弦相似度 | < 0.6 | 标记为 "待审查" |
| 用户修改 | 人工纠偏 | — | 自动收为训练数据 |

**第一版不做全自动标记**——只有用户实际纠正的内容才进入训练数据。Embedding 相似度仅用于辅助标记。

---

## 五、知识库摄入规则

RAG 知识库的内容来源：
1. 船长的分析框架文档（固收产品风险监控模型 v2.0 等）
2. 已确认的训练数据（format_b 框架注入类）
3. GitHub 协作文件中的方法论/规则
4. 船长明确标注为"思想资产"的讨论结论

**不摄入**：客户数据、临时讨论、未确认的草稿。

---

## 六、版本与训练循环

```
训练 MS-vX.Y → 推理检验 → 双模型讨论 → 收集数据 → 训练 MS-vX.(Y+1) → ...
                                    ↑
                              反馈管道在此环中运作
```

---

## 七、RAG 增强推理 — 铁律（2026-08-01 检验发现）

### 注入方式铁律

| ❌ 错误做法 | ✅ 正确做法 |
|-----------|-----------|
| 框架注入 **system prompt** | 框架作为 **[相关知识]** 注入 **user message** |
| "你是分析助手，请用以下框架..." | "[相关知识]：船长的'事后算账'框架认为..." + "\n\n[问题]：..." |
| 一次性 dump 5+ 框架 | 选取最相关的 2-3 个片段 |
| 让模型"按框架分析" | 让模型"基于以下材料回答" |

**原因**（2026-08-01 实测验证）：
- system prompt 中的框架列表 → 模型理解为"操作手册" → 输出"分析指导清单"而非分析本身
- user message 中的知识片段 → 模型理解为"答题材料" → 基于材料给出实质性回答
- 框架不是"规则说明"，是"已知信息"

### 检验流程铁律

每轮检验必须按此顺序：
1. **RAG 检索** → 获取知识片段（如 RAG 不可用，用 AI 已知的船长框架模拟）
2. **框架拼入 user message** → 不是 system prompt
3. **推理** → 获取 RAG 增强的模型回答
4. **比对** → 检查模型是否覆盖了框架关键要素
5. **润色** → yourself-skill
6. **标记差异** → 标注框架缺失/自创概念

---

## 八、跨对话恢复指令

任一涉及反馈管道的对话启动时：
1. `git pull D:\workbuddy\github\`
2. 读取本文件 + `ISSUES_反馈管道.md` + `LOG_反馈管道.md`
3. 检查 ERIC 连通性（SSH Administrator@100.115.45.85）
4. 检查推理服务状态（curl localhost:8000/health）
5. 检查 RAG 服务状态（curl localhost:8001/health）

---

## 九、已知模型缺陷

### 已废弃：MS-v1.0（Mistral-7B）已知问题（2026-08-01）

> 2026-08-02 切换至 Qwen2.5-7B-Instruct（QW-v1.2），以下为历史记录。

| 问题 | 严重度 | 表现 | 改进方向 |
|------|--------|------|---------|
| **数字幻觉** | 🔴 高 | 看到框架中有公式/指标→自动填入看似合理的数字（36%APR、48%不良率、2500 HHI阈值等，均不在RAG框架中） | 训练数据增加"框架定义→不填数字"的示例 |
| **归因浅** | 🔴 高 | 只能描述框架指标，无法完成"识别风险信号"的跃迁 | 增加"框架指标→风险信号映射"类训练对 |
| **截断** | 🟡 中 | max_tokens 不足时直接截断而非自检 | 后续检验设 max_tokens=1200+ |

### MS-v1.1 严重回退（2026-08-01）
- 124 tokens 超短输出，零归因，"指令反射"行为
- 根因：训练数据可能含过短响应 + 过拟合
- **结论：v1.1 不可用**

### 当前：QW-v1.2（Qwen2.5-7B-Instruct，2026-08-02）
- 基座能力显著优于 Mistral-7B，待首次检验

### 检验趋势
```
Mistral-7B 时代：
首轮（裸推理）       ████████████████░░░░ 3.05
二轮（注入错误）     ████████████░░░░░░░░ 2.45  ← system prompt 中毒
三轮（注入正确）     █████████████░░░░░░░ 2.55  ← 恢复但幻觉显著
四轮（v1.1 回退）    ██████░░░░░░░░░░░░░░ 1.38  ← 不可用

Qwen2.5-7B 时代：
QW-v1.2 首轮         ░░░░░░░░░░░░░░░░░░░░ 待检验
```

### RAG 索引架构（v2.0）
- 双 Collection：`fixed_income`（固收831篇）+ `education`（教育18,125篇）
- 异步建索引（threading），不阻塞 HTTP 启动
- QueryRequest 新增 `collection` 参数，默认 "fixed_income"

---

## 十、32B 母数据收集（2026-09-15 定稿，船长 10 点拍板）

> **定位**：船长长期目标「属于自己的 ≥32B 大模型」的母数据收集体系。与 7B 训练并行，7B 吃蒸馏金属、32B 吃原始矿石。

**权威入口（唯一数据目录）**：`D:\workbuddy\32B母数据\`
- `00_规范与索引\32B收集标准.md`（8维+三类+主题划分）
- `00_规范与索引\训练执行指引.md`（CPT/SFT/DPO 分阶段 + 模型兼容）
- `sync_32b.py`（本机搬运）、`count_32b.py`（数据量监测）
- `D:\workbuddy\大模型训练\32B母数据收集方案_定稿_20260915.md`（总纲）

**三类数据**：A 思维资产（8维：触发语境/思维路径/论证压强/纠错轨迹/原话/定论边界/系统关联/元数据）｜B 对话记录（日期标签，剔除操作类）｜C 产出文章。

**三主题**：`01_固收主题` / `02_教育主题` / `03_元认知跨域`。

**搬运机制**：复制快照，源目录永不搬空。三层：① 本机 `collect_dialog.py`（对话记录 projects→32B，关机钩子任务1）+ `sync_32b.py`（思维资产+产出文章，关机钩子任务2）；② N1 侧 `n1_32b_collect.sh`（对话记录兜底，cron `20 3 * * *`，已部署）；③ 关机钩子 `D:\workbuddy\shutdown_hook\`（通用「WorkBuddy退出后执行」机制，权威方案 `同步工具\WorkBuddy退出后执行_通用方案_20260915.md`）。n2（家庭 n1-home）未到货，脚本已备好。

**数据量目标**（count_32b.py 输出）：对话语料 CPT 起步 1亿/充分 10亿 tokens；SFT 样本 1万/5万条；DPO 1000/5000对。

**Skill**：`32b-data-collection`（用户级），`training-data-collector` 已加引用。

**2026-09-15 进展（下午）**：① N1 脚本已部署（cron `20 3`）+ 首次手动验证（857 对话→剔 7→归档 850）；② 本机对话记录已搬（**850 文件/860MB**，按日期归档）；③ 关机钩子框架已写好（`shutdown_hook.cmd`/`shutdown_hook.py`/`collect_dialog.py`）；④ 「退出后执行」通用方案定稿。**关键修正**：对话记录非"AI 无权读"——python 子进程可读 projects（仅 Bash 的 ls/cat 被拒），真正的约束是「当前活跃会话未落盘完整」。

**待办**：① 关机钩子组策略注册（方案C，等船长拍板，含 MaxGPOScriptWait 调整）；② 扩展 NAS 备份含 32B 目录（对话记录 860MB 需打包，NAS 铁律：禁海量小文件直拷）；③ n2 到货后部署 n2 侧；④ 笔记本拉取执行。
