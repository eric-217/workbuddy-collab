# 通知已有对话 — 激活协作机制

> 将以下内容复制粘贴到各自的对话中发送。

---

## 固收分析对话

```
我们建立了一个新的跨对话协作机制。请执行以下操作：

1. cd "D:/workbuddy/github" && git pull
2. 读取 D:/workbuddy/github/MEMORY_SYNC_固收分析.md（当前是空模板）
3. 读取 D:/workbuddy/github/ISSUES_固收分析.md，向我汇报待办事项
4. 读取 D:/workbuddy/github/LOG_固收分析.md（上下文日志模板，了解 LOG 格式）
5. 根据本对话已有的讨论，自行填充：
   - MEMORY_SYNC_固收分析.md（分析框架、方法论、关键结论）
   - LOG_固收分析.md（历史讨论的推理链条、决策上下文）
6. git commit + push

以后每次对话：
- 启动时自动 git pull + 读取 MEMORY_SYNC + ISSUES + LOG
- 完成实质工作后，更新 MEMORY_SYNC（规则变了）+ LOG（推理链）
- LOG 与 MEMORY_SYNC 互补：一个存"过程"，一个存"结论"
```

---

## 境外资产对话

```
我们建立了一个新的跨对话协作机制。请执行以下操作：

1. cd "D:/workbuddy/github" && git pull
2. 读取 D:/workbuddy/github/MEMORY_SYNC_境外资产.md（当前是空模板）
3. 读取 D:/workbuddy/github/ISSUES_境外资产.md，向我汇报待办事项
4. 根据本对话已有的讨论，自行填充 MEMORY_SYNC_境外资产.md（港险配置、CRS合规、跨境方法论、关键结论等）
5. git commit + push

以后每次对话开始时自动执行 git pull + 读取 MEMORY_SYNC + ISSUES。完成实质工作后，如有可复用的规则/知识变更，自动更新 MEMORY_SYNC 并 push。
```

---

## 教育理论对话

```
我们建立了一个新的跨对话协作机制。请执行以下操作：

1. cd "D:/workbuddy/github" && git pull
2. 读取 D:/workbuddy/github/MEMORY_SYNC_教育理论.md（当前是空模板）
3. 读取 D:/workbuddy/github/ISSUES_教育理论.md，向我汇报待办事项
4. 读取 D:/workbuddy/github/LOG_教育理论.md（上下文日志模板，了解 LOG 格式）
5. 根据本对话已有的讨论，自行填充：
   - MEMORY_SYNC_教育理论.md（教育方法论、关键结论、参考资料）
   - LOG_教育理论.md（历史讨论的推理链条、决策上下文）
6. git commit + push

以后每次对话：
- 启动时自动 git pull + 读取 MEMORY_SYNC + ISSUES + LOG
- 完成实质工作后，更新 MEMORY_SYNC（规则变了）+ LOG（推理链）
- LOG 与 MEMORY_SYNC 互补：一个存"过程"，一个存"结论"
```

---

## 财富管理对话（当前对话）

> 当前对话已激活，无需额外通知。LOG 机制仅用于固收分析和教育理论两个主题。

---

## 未来新对话

使用 `跨对话启动命令.md` 中的模板。

---

## 关于 LOG 文件

- **LOG 与 MEMORY_SYNC 的区别**：MEMORY_SYNC 存"规则/结论"，LOG 存"推理过程/决策上下文"
- **当前启用主题**：固收分析、教育理论
- **写入方式**：每次完成实质讨论后，AI 将关键推理链追加到底部
- **维护**：超过 25KB 时自动迁移到 ima 知识库，无需用户确认

---

## ima 知识库（RAG 后端）

### 为什么需要 ima？

LOG 文件的上下文窗口受模型限制——当 LOG 超过 25KB，AI 无法一次读完所有历史推理链。ima 知识库作为 RAG（检索增强生成）后端，按需检索相关片段，不占上下文窗口。

### 架构

```
仓库 LOG 文件（近 6 个月推理链）
        ↓ 超过 25KB
ima 知识库（历史推理链全文检索）
        ↓ AI 需要历史上下文时
按关键词检索 → 精准返回相关推理片段
```

### 凭证配置

本机已配置（2026-07-25），路径：`~/.config/ima/`
- `client_id`：32 字符
- `api_key`：76 字符

所有对话共享同一套凭证，无需重复配置。

### 迁移流程（LOG → ima）

1. LOG 文件增长到约 25KB → AI 自动执行迁移
2. AI 将超过 6 个月的历史推理条目作为笔记上传到 ima 知识库
3. 仓库 LOG 文件精简为近 6 个月内容 + 末尾附"历史推理链已在 ima 知识库「协作中枢」中"标注
4. 后续对话需要历史上下文 → 通过 ima API 的 `search_knowledge` 按关键词检索

### 对话中使用 ima 的规则

- **启动时**：先读仓库 LOG（近期），如需更早的上下文，用 ima 检索
- **写回时**：LOG 追加新推理链（仓库），同时判断是否已达 25KB 门槛
- **自动执行**：达到阈值后自动上传到 ima 并精简仓库 LOG（无需用户确认，LOG 为内部推理链，不含敏感内容）
