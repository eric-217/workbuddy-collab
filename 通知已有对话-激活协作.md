# 通知已有对话 — 激活协作机制

> 将以下内容复制粘贴到各自的对话中发送。

---

## 固收分析对话

```
我们建立了一个新的跨对话协作机制。请执行以下操作：

1. cd "D:/workbuddy/github" && git pull
2. 读取 D:/workbuddy/github/MEMORY_SYNC_固收分析.md（当前是空模板）
3. 读取 D:/workbuddy/github/ISSUES_固收分析.md，向我汇报待办事项
4. 读取 D:/workbuddy/github/LOG_固收分析.md（上下文日志模板，了解 LOG 格式）
5. 根据本对话已有的讨论，自行填充：
   - MEMORY_SYNC_固收分析.md（分析框架、方法论、关键结论）
   - LOG_固收分析.md（历史讨论的推理链条、决策上下文）
6. git commit + push

以后每次对话：
- 启动时自动 git pull + 读取 MEMORY_SYNC + ISSUES + LOG
- 完成实质工作后，更新 MEMORY_SYNC（规则变了）+ LOG（推理链）
- 需要查找历史资料 → 先搜仓库 LOG（近 12 个月），未命中则用 `search_knowledge` 搜 ima 知识库「固收分析」文件夹
- LOG 与 MEMORY_SYNC 互补：一个存"过程"，一个存"结论"
- LOG 自动维护：写回后检查文件大小，≥ 50KB 自动精简（保留近 12 个月，超期上传 ima）
```

---

## 境外资产对话

```
我们建立了一个新的跨对话协作机制。请执行以下操作：

1. cd "D:/workbuddy/github" && git pull
2. 读取 D:/workbuddy/github/MEMORY_SYNC_境外资产.md（当前是空模板）
3. 读取 D:/workbuddy/github/ISSUES_境外资产.md，向我汇报待办事项
4. 根据本对话已有的讨论，自行填充 MEMORY_SYNC_境外资产.md（港险配置、CRS合规、跨境方法论、关键结论等）
5. git commit + push

以后每次对话开始时自动执行 git pull + 读取 MEMORY_SYNC + ISSUES。完成实质工作后，如有可复用的规则/知识变更，自动更新 MEMORY_SYNC 并 push。需要查找历史资料时，用 `search_knowledge` 搜 ima 知识库「境外资产」文件夹。
```

---

## 教育理论对话

```
我们建立了一个新的跨对话协作机制。请执行以下操作：

1. cd "D:/workbuddy/github" && git pull
2. 读取 D:/workbuddy/github/MEMORY_SYNC_教育理论.md（当前是空模板）
3. 读取 D:/workbuddy/github/ISSUES_教育理论.md，向我汇报待办事项
4. 读取 D:/workbuddy/github/LOG_教育理论.md（上下文日志模板，了解 LOG 格式）
5. 读取 D:/workbuddy/github/通知已有对话-激活协作.md 中的「ima 完整配置」章节
6. 根据本对话已有的讨论，自行填充：
   - MEMORY_SYNC_教育理论.md（教育方法论、关键结论、参考资料）
   - LOG_教育理论.md（历史讨论的推理链条、决策上下文）
7. git commit + push

以后每次对话：
- 启动时自动 git pull + 读取 MEMORY_SYNC + ISSUES + LOG + 通知文件 ima 配置
- 完成实质工作后，更新 MEMORY_SYNC（规则变了）+ LOG（推理链）
- 有新的文章想法/案例 → 用 append_doc 追加到 ima 笔记（note_id: 7486777623333695）
- 需要查找历史资料 → 先搜仓库 LOG（近 12 个月），未命中则用 search_knowledge 搜 ima 知识库
- LOG 自动维护：写回后检查文件大小，≥ 50KB 自动精简（保留近 12 个月，超期上传 ima）
```

---

## 财富管理对话（当前对话）

> 当前对话已激活，无需额外通知。需要查找历史资料时，用 `search_knowledge` 搜 ima 知识库「财富管理」文件夹。LOG 机制仅用于固收分析和教育理论两个主题。

---

## 财商教育对话（当前对话）

> ⚠️ 本对话与「教育理论对话」共享教育理论大框架（内在资源体系），但财商教育已独立为子主题（13批精读完成+独立知识库+独立写作方向），故建立独立同步文件。

```
我们建立了一个新的跨对话协作机制。请执行以下操作：

1. cd "D:/workbuddy/github" && git pull
2. 读取 D:/workbuddy/github/MEMORY_SYNC_财商教育.md（当前是空模板）
3. 读取 D:/workbuddy/github/ISSUES_财商教育.md，向我汇报待办事项
4. 读取 D:/workbuddy/github/LOG_财商教育.md（上下文日志模板，了解 LOG 格式）
5. 读取 D:/workbuddy/github/通知已有对话-激活协作.md 中的「ima 完整配置」章节
6. 根据本对话已有的讨论，自行填充：
   - MEMORY_SYNC_财商教育.md（财商教育方法论、核心概念、关键结论、参考资料）
   - LOG_财商教育.md（历史讨论的推理链条、决策上下文）
7. git commit + push

以后每次对话：
- 启动时自动 git pull + 读取 MEMORY_SYNC + ISSUES + LOG + 通知文件 ima 配置
- 完成实质工作后，更新 MEMORY_SYNC（规则变了）+ LOG（推理链）
- 需要查找历史资料 → 先搜仓库 LOG（近 12 个月），未命中则用 `search_knowledge` 搜 ima 知识库
- LOG 自动维护：写回后检查文件大小，≥ 50KB 自动精简（保留近 12 个月，超期上传 ima）
```

---

## 未来新对话

使用 `跨对话启动命令.md` 中的模板。

---

## LOG 文件自动维护规则（⭐ 50KB + 12个月 + 全自动）

### 基本参数

| 参数 | 值 | 说明 |
|------|---|------|
| **触发阈值** | 50KB | LOG 文件大小 ≥ 50KB 时自动触发精简 |
| **保留期限** | 12 个月 | 仓库 LOG 仅保留最近 12 个月的推理链条目 |
| **迁移目标** | ima 知识库「协作中枢」 | 超过 12 个月的历史条目上传为 ima 笔记 |
| **执行方式** | **全自动** | AI 自主检测→执行，不等待、不询问、不提醒用户 |

### 为什么是 50KB + 12 个月？

- 50KB：约 15-25 条推理链的容量，AI 可以一次读完所有近期上下文
- 12 个月：超过一年的推理链通常已完成其决策参考价值，转为"历史档案"由 ima 按需检索

### LOG 写入规则

- **时机**：每次完成实质性讨论后，AI 将关键推理链追加到底部
- **格式**：`## YYYY-MM-DD 话题标题` → 背景 → 分析路径 → 决策 → 可复用方法
- **追加到底部**，不覆盖历史内容
- 写入后 `git commit -m "LOG[主题]：[简述]" && git push`

### LOG 读取规则

- 对话启动时，git pull 后读取本主题 LOG 文件
- 如果 LOG ≤ 50KB，读取全文
- 如果 LOG > 50KB（不应出现，但做容错），读取最近 15 条条目
- 新讨论涉及历史问题时，先搜仓库 LOG（近 12 个月），找不到再用 ima 检索

### 自动精简流程（AI 自主执行，无需用户介入）

```
每次写回 LOG 后
    ↓
检查 LOG 文件大小
    ↓  ≥ 50KB ？
    ├── 否 → 正常结束
    └── 是 → 自动执行：
         1. 解析所有条目，按日期分组
         2. 超过 12 个月的条目 → 上传到 ima 知识库「协作中枢」
         3. 仓库 LOG 精简为近 12 个月内容
         4. 末尾追加 "> 历史推理链（YYYY-MM 之前）已在 ima 知识库「协作中枢」中"
         5. git commit -m "LOG[主题]：自动精简，历史条目迁移ima" && git push
         6. 不告知用户（全自动后台操作）
```

### 当前启用主题

| 主题 | LOG 文件 |
|------|---------|
| 固收分析 | `LOG_固收分析.md` |
| 教育理论 | `LOG_教育理论.md` |
| 财商教育 | `LOG_财商教育.md` |

> 其他主题目前不启用 LOG 机制。如需为其他主题启用，用户说"给XX主题加LOG"即可。

---

## 写回仓库的时机与流程（⭐ 收集→分类→推送）

### 推送时机

**对话结束时统一推送**，不在对话中途逐条推送。

| 场景 | 时机 |
|------|------|
| 规则/协作约定变更 | 对话结束时，与本次所有变更一起推送 |
| LOG 推理链追加 | 对话结束时，与本次所有变更一起推送 |
| ISSUES 增减 | 对话结束时，与本次所有变更一起推送 |
| MEMORY_SYNC 更新 | 对话结束时，与本次所有变更一起推送 |

> 例外：极少数场景需要即时推送（如跨对话紧急传递一条关键信息），由用户主动要求。

### 推送前流程（收集→分类→落文件→推送）

对话快结束或用户说"推"时，AI 必须执行以下 4 步：

```
第1步：收集
    ├── 扫描本次对话的全部产出
    ├── 识别哪些内容具有"跨对话复用价值"
    └── 列出待入库清单

第2步：分类
    ├── 可复用的规则/方法论/约定 → MEMORY_SYNC_主题名.md
    ├── 系统级规则（跨主题通用） → MEMORY_SYNC.md + ~\.workbuddy\MEMORY.md
    ├── 协作工作流约定 → 协作规则_主题名.md
    ├── 任务/待办/已完成 → ISSUES_主题名.md
    └── 推理链条/决策上下文 → LOG_主题名.md（仅启用 LOG 的主题）

第3步：落文件
    ├── 先 git pull（确保基于最新版本编辑）
    ├── 逐一写入对应文件
    ├── 检查：有没有重复？有没有跟已有内容冲突？有没有客户隐私？
    └── 确认无误后进入第4步

第4步：推送
    └── git add -A && git commit -m "描述" && git push
```

### 分类判断标准

| 内容类型 | 判断方法 | 落谁家 |
|---------|---------|--------|
| "以后遇到XX情况就按这个来" | 可复用的规则 | MEMORY_SYNC |
| "这个结论下次讨论XX时要用到" | 跨对话的结论 | MEMORY_SYNC |
| "我们约定以后XX这样做" | 协作工作流 | 协作规则 |
| "记一条待办/XX做完了" | 任务追踪 | ISSUES |
| "刚才讨论的推理路径是这样的…" | 过程记录 | LOG |
| "帮我记一下这个灵感和想法" | 个人备忘 | ima 笔记 / 本地 MEMORY |
| 客户的资产/保单/姓名 | 隐私数据 | 仅本地 MEMORY，禁止进仓库 |

### 禁止的行为

- ❌ 写了一行就 git push——碎片化推送浪费且难以回溯
- ❌ 跳过"收集→分类"直接写文件——容易放错位置或遗漏
- ❌ 不 git pull 直接 push——可能覆盖他人的更新
- ❌ 把客户隐私写进任何仓库文件

---

## ima 完整配置（知识库 + 笔记）

### 常识问答

**Q: ima 知识库的免费存储空间有多大？**

基础 30 GB（单设备），下载双端（电脑+手机）+20 GB = 50 GB，邀请新用户最高再+50 GB = 100 GB。学生/教师认证直达 100 GB。

对于文本类资料（平均 50KB/篇），30 GB 可存储约 60 万篇文章——基本无限。

**Q: 把文章放在 ima 知识库里读，会比本地更快吗？**

不会。读单篇文件时，本地直接打开最快。ima 的真正优势是：

- **全文语义搜索**：42 篇文章里搜一个概念，秒出结果，不需要逐一打开
- **不占上下文窗口**：搜索只返回相关片段，不会把整篇文章塞进 AI 对话
- **跨对话持久化**：所有对话共享同一知识库，任何一个对话都能检索

**结论**：本地文件 = 精确阅读；ima = 跨主题检索。两者互补，不替代。

---

### 一、知识库架构

**知识库**：「李春华17827599491的知识库」— 当前 42 篇文章，全部在根目录。

**按 4 个协作主题建子文件夹**（需在 ima 桌面客户端手动操作，API 不支持建文件夹和移动文件）：

| 文件夹 | 文章数 | 内容范围 |
|--------|--------|---------|
| 📁 固收分析/ | ~3 | 固收产品风险、P2P 案例、消费金融 |
| 📁 境外资产/ | ~18 | 香港金融、稳定币、RWA、美联储 |
| 📁 财富管理/ | ~19 | 银行监管、房地产、保险、宏观政策 |
| 📁 教育理论/ | ~5 | 认知提升、教育方法、AI 学习 |

> 📋 详细归类清单见附录 A。在 ima 桌面客户端 → 知识库 → 右键新建文件夹 → 拖拽文章归位。（约 5 分钟手动操作）

#### 对话中使用知识库的规则

| 场景 | 操作 |
|------|------|
| 启动时 | 不自动读知识库（内容太多），只在需要时检索 |
| 讨论中需要查历史资料 | 用 `search_knowledge` 按关键词检索 |
| 用户说"把这篇加到知识库" | 用 `import_urls` 添加到对应文件夹 |
| 用户上传文件需要存档 | 用 `add_knowledge` 上传到知识库 |
| 用户说"帮我搜一下知识库里关于XX的文章" | `search_knowledge` → 返回相关片段摘要 |

#### 知识库支持的添加方式

- 🌐 网页/公众号文章 → `import_urls`（直接传 URL，ima 自动抓取内容）
- 📄 本地文件（PDF/Word/PPT/Markdown 等 20+ 格式）→ 完整上传流程
- 📝 ima 笔记 → 笔记创建后添加到知识库

---

### 二、笔记 — 长文章草稿迭代

**启用范围**：仅教育理论主题（按用户要求，其他主题暂不启用）

**使用场景**：教育理论书稿文章通常需要多轮迭代——第一天写核心观点，第二天补充案例，周末整合成完整文章。用 ima 笔记的 `append_doc`（追加模式），每次对话往同一篇笔记追加新内容，逐步积累。

**工作流**：
```
教育理论对话中
    ↓
用户有新想法/案例 → AI 调用 append_doc 追加到对应笔记
    ↓
积累到足够素材 → AI 整合为完整文章
    ↓
发布前 → 导出为公众号 HTML
```

**当前已有笔记**：
| 笔记 ID | 标题 | 用途 |
|---------|------|------|
| `7486777623333695` | 教育理论 长文章草稿本 | 总索引 + 所有文章草稿 |

> 📋 建议在 ima 桌面客户端创建笔记本「教育理论-草稿」，将笔记归入此笔记本。（API 不支持创建笔记本）

**对话中追加草稿示例**：
> 用户："把刚才讨论的'事后总结法'案例加到需求驱动那篇草稿里"
> AI：调用 `append_doc` → 追加到笔记 `7486777623333695`

---

### 三、凭证配置

本机已配置（2026-07-25），路径：`~/.config/ima/`（`client_id` + `api_key`）。所有对话共享同一套凭证。

---

### 四、API 能力边界

| 操作 | API 支持？ | 替代方式 |
|------|-----------|---------|
| 搜索知识库内容 | ✅ `search_knowledge` | — |
| 添加网页/公众号到知识库 | ✅ `import_urls` | — |
| 上传文件到知识库 | ✅ `add_knowledge` | — |
| 创建/搜索/追加笔记 | ✅ `import_doc` / `append_doc` | — |
| 在知识库建文件夹 | ❌ | 桌面客户端手动操作 |
| 移动知识库文章 | ❌ | 桌面客户端手动拖拽 |
| 创建笔记本 | ❌ | 桌面客户端手动操作 |

---

### 附录 A：知识库文章归类清单

> 在 ima 桌面客户端操作：知识库 → 右键「新建文件夹」→ 根据下表拖拽文章归位。

**📁 固收分析/**（3 篇）
1. 宜信百亿类固收产品调查
2. 宜信内部员工的独白，唐宁的底色与宜信的困局（一）| 锐评
3. 一场新的P2P骗局，正在酝酿？

**📁 境外资产/**（18 篇）
4. 香港证监会：香港持牌公司可继续为存量内地客户提供服务，但不能在内地境内提供
5. 风暴眼中的汇丰：让中国富豪全球花钱自由，还有华尔街资本大佬撑腰
6. 1700亿香港地产豪门，化债渡劫
7. 稳定币迅速被多国接纳，机构：或成美元信用体系崩塌的催命符
8. RWA注册登记平台在香港上线，推动概念验证走向规模化应用
9. 刚刚！RWA注册登记平台正式在香港上线
10. "疯狂"的赴港RWA
11. 稳定币，蚂蚁下了一步先棋
12. 了解稳定币，这篇就够了！
13. 国内多家企业宣布入局稳定币，跨境支付新风口到来
14. 香港的稳定币准备怎么玩？
15. 全球监管"泼凉水"，稳定币祛魅
16. 全球热议稳定币！一文读懂
17. 美联储重启降息，对中产阶级的职业和财富意味着什么？
18. 美联储，选择救市
19. 下半年超预期机会可能来自"顺周期"！刘煜辉最新交流，详谈稳定币及反内卷
20. 信号灯已亮，全球货币储备大转向！

**📁 财富管理/**（19 篇）
21. 国家金融监督管理总局决定对众邦银行实施接管
22. 金融法草案首次提请全国人大常委会会议审议
23. 央行行长宣布：将出台六项政策措施
24. 重磅，人民银行等七部门联合发布
25. 央行发布：支付宝、财付通将由中国人民银行总行负责反洗钱监管 | 大鱼财经
26. 资本市场，大调整！23万亿元行业，迎新规
27. 人民币存贷计息或将迎来五大重要调整
28. 国有大行响应消费贷贴息：将降低居民信贷成本、助力提振消费
29. 这家消金股权迎变更！
30. 这些银行、消金公司纷纷入局文旅场景
31. 银行，真疯了！
32. 全国首个破产人和她背后的个人破产制度
33. 百万存款"围城"：高收入≠高财商，年轻新贵们的财富焦虑
34. 35家寿险公司，谁的投资能力更胜一筹？
35. 《求是》再提房地产，信息量很大
36. 我国地方政府专项债务首次突破40万亿元，怎么看？
37. 达利欧最新预测：未来5年，世界的五大巨变

**📁 教育理论/**（5 篇）
38. 读懂《道德经》这句话，你就明白了赚钱的底层法则
39. 解析中国"高认知穷人"现象：错位、成因与生存建议
40. 人到中年，你的精神状态怎么样｜一份对2556位"夹心中年"的调查
41. 国庆这8天，我发现和AI辩论才是最高效的学习方式
42. 任正非最新讲话！1.4万字全文，信息量很大
